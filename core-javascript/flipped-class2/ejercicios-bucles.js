//. Escribir un programa que muestre el eco de todo lo que el usuario
// introduzca hasta que el usuario escriba “salir” que terminará

let input_text = window.prompt("Escribe un texto, si escribes salir, se terminará el programa");

console.log(imput_text);
alert(input_text);
document.write(input_text);